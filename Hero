  {/* Hero */}
        <section className="relative overflow-hidden">
          <div className="absolute inset-0 -z-10" style={{ background: `linear-gradient(180deg, ${COLORS.primary}0f 0%, transparent 60%)` }} />
          <div className="mx-auto max-w-6xl px-4 py-16 md:py-24 grid md:grid-cols-2 gap-10 items-center">
            <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ duration: 0.5 }}>
              <h1 className="text-4xl md:text-5xl font-extrabold tracking-tight leading-tight">
                Learn Fast. <span style={{ color: COLORS.accent }}>Act Faster.</span>
              </h1>
              <p className="mt-4 text-lg text-slate-600">
                Launch your EMS career with <strong>911EDGE</strong> – a modern EMT training program combining hybrid learning, expert instructors, and scenario‑based skills labs to prepare you for the NREMT and real‑world patient care.
              </p>
              <div className="mt-6 flex flex-col sm:flex-row gap-3">
                <Button size="lg" asChild className="text-white" style={{ backgroundColor: COLORS.accent }}>
                  <a href="#apply">Reserve Your Seat</a>
                </Button>
                <Button size="lg" variant="outline" asChild>
                  <a href="#syllabus">View Syllabus</a>
                </Button>
              </div>
              <div className="mt-6 flex flex-wrap items-center gap-4 text-sm text-slate-700">
                <div className="flex items-center gap-2"><Calendar className="h-4 w-4"/> January 2026 </div>
                <div className="flex items-center gap-2"><MapPin className="h-4 w-4"/> Lake Monticello Rescue Squad · Palmyra, VA </div>
              </div>
            </motion.div>

            <motion.div initial={{ opacity: 0, scale: 0.98 }} whileInView={{ opacity: 1, scale: 1 }} viewport={{ once: true }} transition={{ duration: 0.5, delay: 0.1 }}>
              <div className="relative">
                <div className="absolute inset-0" style={{ background: `${COLORS.gold}33`, filter: "blur(64px)", borderRadius: 9999 }} />
                <Card className="rounded-2xl shadow-xl">
                  <CardHeader>
                    <CardTitle className="flex items-center gap-2"><GraduationCap className="h-5 w-5"/> 911EDGE Highlights</CardTitle>
                  </CardHeader>
                  <CardContent className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    {[
                      "Hybrid format (online + hands‑on)",
                      "NREMT‑focused exam prep",
                      "Small‑group skills coaching",
                      "Clinical/ride‑along opportunities",
                      "CE options for providers",
                      "Experienced, VA OEMS‑credentialed faculty",
                    ].map((item) => (
                      <div key={item} className="p-3 rounded-xl border bg-white/70 shadow-sm text-sm">{item}</div>
                    ))}
                  </CardContent>
                </Card>
              </div>
            </motion.div>
          </div>
        </section>
